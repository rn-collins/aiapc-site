import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const read=p=>readFile(new URL("../"+p,import.meta.url),"utf8");test("prelaunch sales boundary",async()=>{const s=await read("bundle.html");assert.match(s,/No checkout is open/);assert.doesNotMatch(s,/\$1,024\+/)});test("privacy and analytics align",async()=>{const s=await read("index.html");assert.doesNotMatch(s,/_vercel\/insights/)});test("lead collection is retired",async()=>{const s=await read("api/lead.js");assert.match(s,/status:410/)})